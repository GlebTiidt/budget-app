{
  "name": "budget-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx --env-file=.env.local src/app/main.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test 'tests/**/*.test.ts'"
  },
  "dependencies": {
    "@notionhq/client": "^4.0.0",
    "grammy": "^1.38.1",
    "openai": "^6.48.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  }
}
